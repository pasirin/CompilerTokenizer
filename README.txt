file code để chạy phần giữa kỳ được đặt trong file ./src/Main.java
file các module để sử dụng cho bài cuối kỳ là Main2.java (file nhận dữ liệu và chuyển hóa thành một mảng các token hợp lệ)
file Tree.java thử chuyển hóa các token về dạng phù hợp cho LL(1) bằng việc xây dựng cây
file LL1.java duyệt qua mảng các token, sử dụng bảng parser để xác minh các token đó có thỏa mãn LL(1) hay không
input của file cho vào trong file input.vc và đầu ra
là file output.vctok
file demo.dat là file dữ liệu được sử dụng bởi model
Khuyến khích sử dụng intellij để chạy code 